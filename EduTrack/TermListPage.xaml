<?xml version="1.0" encoding="utf-8" ?>
ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="EduTrak.TermListPage">
<ContentPage.Content>
    <StackLayout Padding="10">
        <Label Text="Terms" FontSize="Large" />
        <Button Text="Add New Term" Clicked="OnAddTermClicked" />
        <ListView x:Name="TermsListView">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextCell Text="{Binding Name}" Detail="{Binding StartDate}" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>
</ContentPage.Content>
</ContentPage>

<!--The following is default code for .NET MAUI applications--> 
<!--<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="EduTrack.TermListPage"
             Title="TermListPage">
    <VerticalStackLayout>
        <Label 
            Text="Welcome to .NET MAUI!"
            VerticalOptions="Center" 
            HorizontalOptions="Center" />
    </VerticalStackLayout>
</ContentPage>-->